# `react-button`

Button 是按钮组件，提供多种尺寸、变体和颜色选项。

## 安装

```sh
pnpm add @sea-lion/react-button
# 或者
npm install @sea-lion/react-button
yarn add @sea-lion/react-button
```

## 使用

在代码中引入组件：

```tsx
import { Button } from '@sea-lion/react-button';
```

### 基本用法

```jsx
<Button>默认按钮</Button>
<Button variant="solid" color="blue">主要按钮</Button>
<Button variant="outline">线框按钮</Button>
```

### 尺寸

支持尺寸 `"1"` | `"2"` | `"3"` | `"4"`。

### 变体

支持变体：`classic` | `solid` | `soft` | `surface` | `outline` | `ghost`。

## 查看更多

查看[文档](https://gitee.com/summer_sleep/ui-lib-monorepo-template)获取更多信息。
